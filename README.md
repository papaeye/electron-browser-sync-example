# electron-browser-sync-example

An example of reloading Electron App with Browsersync.

## Getting Started

1. Run `npm install`.
2. Run `gulp watch`.
3. Edit and save `renderer.js` and/or `index.html`, then Browsersync should reload the BrowserWindow.
